# Ruby Core Production Environment Configuration
# Copy this file to .env and fill in values
# IMPORTANT: Never commit .env files with real values to Git!
#
# Production secrets MUST come from Vault (ADR-0015)
# This file is for non-sensitive configuration only.

# =============================================================================
# Vault Configuration (ADR-0015)
# =============================================================================
# Production Vault server address (NOT dev mode!)
VAULT_ADDR=https://vault.your-domain.local:8200

# Service authentication method (use AppRole or Kubernetes auth in production)
# Do NOT use root tokens in production!
# VAULT_ROLE_ID=<from-vault-approle-setup>
# VAULT_SECRET_ID=<from-vault-approle-setup>

# =============================================================================
# NATS Configuration
# =============================================================================
# NATS server URL (TLS required)
NATS_URL=tls://nats.your-domain.local:4222

# Path to NATS CA certificate
NATS_CA_FILE=/etc/ruby-core/certs/ca.pem

# =============================================================================
# Service Configuration
# =============================================================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Environment name
ENVIRONMENT=production

# =============================================================================
# Release Version
# =============================================================================
# Used by docker-compose to pull correct image tags
VERSION=v0.1.0

# GitHub repository for image paths
GITHUB_REPOSITORY=your-org/ruby-core

# =============================================================================
# Clock Skew Tolerance (ADR-0026)
# =============================================================================
# Tolerance window in milliseconds for time-sensitive operations
CLOCK_SKEW_TOLERANCE_MS=1000
